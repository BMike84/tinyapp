<header>  
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <!-- if wrong -->
    <% if (!user) { %>
      <a class="btn btn-primary" style="margin-right: 15px;" href="/login">  Login  </a>
      <a class="btn btn-primary" href="/register"> Register </a>
    <% } else { %>
      <form class="form-inline my-1"  method="POST" action="/logout">
        <div>
          <!-- if wrong -->
          Logged in as: <%= user.email %>
          <button type="submit" class="btn btn-outline-dark" style="margin-left: 10px;">Logout</button>
        </div>
      </form>
    <%} %>
  </nav>
</header>